<h1>Listing uploads</h1>

<table>
  <tr>
    <th></th>
    <th></th>
  </tr>

<% @uploads.each do |picture| %>
  <tr>
    <td><%= image_tag picture.content %></td>
    <td><%= link_to 'Show', "/uploads/#{picture.to_param}" %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Upload', new_upload_path %>
