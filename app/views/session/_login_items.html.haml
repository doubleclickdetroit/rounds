- if current_user
	= image_tag current_user.image_path || 'dne.png'
	= "| Logged in as #{current_user.name} | "
	= link_to "Sign out", signout_path

- else
	= link_to "Sign in with Facebook", '/auth/facebook/'
